name: View perms

on: workflow_dispatch

jobs:
    view-perms:
        env:
            GH_TOKEN: ${{github.token}}
        permissions:
                contents: 'read'
        runs-on: ubuntu-latest
        steps:
            - run: gh auth status